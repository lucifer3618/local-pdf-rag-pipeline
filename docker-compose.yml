# docker-compose.yml
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./pdfs:/app/pdfs          # mount PDFs
      - ./chroma_db:/app/chroma_db # persist ChromaDB
    environment:
      - OLLAMA_HOST=host.docker.internal:11434